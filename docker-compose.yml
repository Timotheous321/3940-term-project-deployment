services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=my-super-secret-key

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend